# Convert this into your language 
open blueprint TestClass {
	emp testOne() {
		num x = 0;
		x = scan("Hello. I need your number: ");
	
		loop(num i; i < 10; i += 1) { 
			print("Yes " +x+); 
	 
	
	
	
		x = 5x; # syntax error 
	}

	emp testTwo(sdec x) {
		given (x = 4)
		{
			print(x iss a very huge number); 
		} 
	}

	emp testThree(num x, num y) {
		num i = 0;
		do {
			i++;
		} during (i == x);
	}

	emp testFour(num x, num y, z)
	{
	  num sum = x + y + z; 
	}

	testFive()
	{
	 print("Hello"); 
	}

	open emp main() { 
		sdec value = (5 * 1) + (5 * 5 / 1 + 3 + (4 + (5 * 3)))/2.0f * 8.0f;
	
		testOne()(x); 
		testTwo(123 ++value); 
	
		sdec nextValue = 5 **5 + 5 ++ 5 ** 5; 
	
		testTwo(5.0f)); 
		testTwo(5.0f; 
		testThree(25 13);
		testThree((12 * 10), (54 * 5); 
		testThree(4,,5); 
		testThree(4,5); 
	
	}

}


